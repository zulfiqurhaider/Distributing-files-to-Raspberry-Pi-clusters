# Distributing files to Pi-clusters
This is a simple python script that reads a csv file from Desktop, splits it into three part and distribute them into different Raspberry Pi cluster connected with the desktop through Ethernet swwitch.

After distributing the file we command those 3 Raspberry Pi to run a simple "Sentiment Analysis" script to calculate the sentiment score of each review in the dataset.
